<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="@string/max_update_file_length_pref_key"
        android:title="@string/pref_title_set_max_update_file_length"
        android:defaultValue="0"
        android:entries="@array/pref_max_update_file_length_titles"
        android:entryValues="@array/pref_max_update_file_length_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
